# P1 / P2 / P3 评估协议概览

仓库内三条严谨评估路径，用途和成本各不相同，**不能混成一个分数来解释**。

---

## 总览

| 协议 | 配置 | 用途 | 成本 | 适合回答的问题 |
|---|---|---|---|---|
| **P1** | `configs/protocols/p1_repeated_group_kfold.yaml` | 严格重训比较 | 高 | 这条路线平均水平和 seed/fold 稳定性如何？ |
| **P2** | `configs/protocols/p2_pseudo_public_holdout.yaml` | subject holdout + crop 压力测试 | 中 | 模型对未知 subject、单 crop、worst crop 是否稳？ |
| **P3** | `configs/protocols/p3_nested_selection.yaml` | 带参数选择的最终验证 | 很高 | 调参路线是否在外层 test 上仍然成立？ |

### 统一红线

- 不使用 public/private labels
- 不使用 leaderboard feedback
- 不生成 final submission
- 不修改 split
- `subject_id` / `trial_id` 只能用于分组、对齐、Top-4、审计

---

## P1：Repeated Group K-Fold

### 核心逻辑

```
多个 seed × 多个 subject-group fold
每个 fold 从零训练
每个 held-out subject 用 exact_single_crop_expected_BA 评估
```

### 配置参数

```yaml
# configs/protocols/p1_repeated_group_kfold.yaml
protocol: p1_repeated_group_kfold
subject_group_split: true
n_splits: 5
seeds: [42, 123, 456, 789, 1024]
primary_metric: exact_single_crop_expected_BA
```

### Split 策略

- 按 cohort（DEP / HC）分组，每个 cohort 内做 stratified group k-fold
- 每折从 train pool 中再划出 20% 作为 val
- 总共 `5 seeds × 5 folds = 25` 次完整训练

### 源码入口

| 层 | 位置 |
|---|---|
| Runner 构建 | [runner.py:140-159](src/hust_bci_er/evaluation/protocols/runner.py#L140-L159) |
| Split 逻辑 | [subject_splits.py:127-157](src/hust_bci_er/evaluation/protocols/subject_splits.py#L127-L157) |

### 什么时候用

- 比较两个路线的平均性能
- 看 seed / fold 波动（`fold_std`、`subject_std`）
- 给 candidate 提供主要 CV 证据

### 不要用来做什么

- 不适合快速探索
- 不适合一边看结果一边调参数后继续声称是独立验证

### 怎么跑

```bash
# 生成 plan（不训练）
python scripts/plan_evaluation_protocol.py \
  --protocol p1 \
  --route-config configs/routes/models/ea_deformer.yaml

# materialize runner manifest
python scripts/run_evaluation_protocol.py \
  --protocol p1 \
  --route-config configs/routes/models/ea_deformer.yaml \
  --run-dir outputs/protocol_runs/<run_id>

# 带真实数据 + 受控执行
python scripts/run_evaluation_protocol.py \
  --protocol p1 \
  --route-config configs/routes/models/fixed_crop_ea_fbcnet.yaml \
  --run-dir outputs/protocol_runs/p1_fbcnet_<run_id> \
  --data-root scratch/local_data/hust_bci_er_train/训练集 \
  --device cuda \
  --seed 42 \
  --n-folds 5

# smoke 执行
python scripts/run_evaluation_protocol.py \
  --protocol p1 \
  --route-config configs/routes/models/toy_eegnet.yaml \
  --run-dir outputs/protocol_runs/toy_p1 \
  --seed 42 --n-folds 2 \
  --execute --execute-gate smoke --max-execute-jobs 1
```

### 结果解读

```
更像"路线平均能力"
重点看 mean、seed_std、fold_std、subject_std
```

---

## P2：Pseudo-Public Holdout

### 核心逻辑

```
先按 subject 划出一组 holdout subjects（20%）
只用非 holdout subjects 训练
训练一次，复用 checkpoint
在 holdout subjects 上分别测试 7 种 crop policy
```

### 配置参数

```yaml
# configs/protocols/p2_pseudo_public_holdout.yaml
protocol: p2_pseudo_public_holdout
subject_group_split: true
holdout_subject_fraction: 0.2
crop_policies: [crop1, crop2, crop3, crop4, crop5, random, worst]
primary_metric: exact_single_crop_expected_BA
```

### Crop Policy 规则

| Policy | 行为 |
|---|---|
| `crop1` – `crop5` | 固定 crop index 0-4 |
| `random` | 每个 trial 用 `numpy.default_rng(train_seed)` 随机选 crop |
| `worst` | label-aware 压力测试，选择使 metric 最低的 assignment |

### Job 结构

```
1 × train_holdout_model  →  checkpoint.pt（可复用）
7 × evaluate_holdout_crop_policy  →  复用同一 checkpoint
```

### 源码入口

| 层 | 位置 |
|---|---|
| Runner 构建 | [runner.py:162-194](src/hust_bci_er/evaluation/protocols/runner.py#L162-L194) |
| Split 逻辑 | [subject_splits.py:160-206](src/hust_bci_er/evaluation/protocols/subject_splits.py#L160-L206) |

### 什么时候用

- 检查模型是否对 crop 位置敏感
- 检查 worst crop 下是否崩溃
- 模拟真实未知 subject 的压力测试
- 对比 Top-4 和 no-Top4 行为

### 不要用来做什么

- 不应单独作为最终 candidate 主证据
- 不应根据 holdout 结果反复调参后把同一 holdout 当独立测试

### 怎么跑

```bash
# 生成 plan
python scripts/plan_evaluation_protocol.py \
  --protocol p2 \
  --route-config configs/routes/models/ea_deformer.yaml
```

### 结果解读

```
更像"真实推理压力测试"
重点看 normal crop 均值、worst crop、random crop、no-Top4 对照
```

---

## P3：Nested Selection

### 核心逻辑

```
outer fold 只负责最终 test
inner fold 只负责选择参数
选好参数后，outer final 在 outer train 范围内重新训练 final artifact
final artifact 只在 outer test 上评估
selected params / selection artifacts 会写进 final manifest
```

### 配置参数

```yaml
# configs/protocols/p3_nested_selection.yaml
protocol: p3_nested_selection
subject_group_split: true
outer_splits: 5
inner_splits: 3
primary_metric: exact_single_crop_expected_BA
```

### 三层嵌套结构

```
Outer fold 0
├── Inner fold 0  →  参数候选 0, 1, 2... 各自训练 + 写 selection_metrics.json
├── Inner fold 1  →  同上
├── Inner fold 2  →  同上
└── Outer Final    →  按 inner mean 选 best param_index，在 outer train 重训，outer test 评估

... Outer fold 1-4 同理
```

### Job 计算公式

```
inner_select jobs = outer_folds × inner_folds × param_candidates
outer_final_retrain jobs = outer_folds
```

### 源码入口

| 层 | 位置 |
|---|---|
| Runner 构建 | [runner.py:196-254](src/hust_bci_er/evaluation/protocols/runner.py#L196-L254) |
| Outer split | [subject_splits.py:209-242](src/hust_bci_er/evaluation/protocols/subject_splits.py#L209-L242) |
| Inner split | [subject_splits.py:245-289](src/hust_bci_er/evaluation/protocols/subject_splits.py#L245-L289) |
| 参数网格展开 | [params.py:82-125](src/hust_bci_er/evaluation/protocols/params.py#L82-L125) |

### 什么时候用

- 路线包含可选参数（权重、dropout、融合参数等）
- 想证明"参数选择过程本身"没有泄露 outer test
- 最终 scientific review 前做最严格确认

### 不要用来做什么

- 不适合日常快速探索
- 不适合 GPU 资源紧张时先跑全量
- **不允许 outer test 参与参数选择**

### 怎么跑

```bash
# 生成 plan（用 --grid-size 估算工作量）
python scripts/plan_evaluation_protocol.py \
  --protocol p3 \
  --route-config configs/routes/models/ea_deformer.yaml \
  --grid-size ea_deformer=6

# materialize（必须传 --param-grid）
python scripts/run_evaluation_protocol.py \
  --protocol p3 \
  --route-config configs/routes/models/sliding_window_eegnet.yaml \
  --param-grid sliding_window_eegnet=configs/search/spaces/sliding_baseline.yaml \
  --run-dir outputs/protocol_runs/p3_sliding_window_eegnet_<run_id>
```

### 参数网格格式

支持两种形状：

```yaml
# 1. 笛卡尔积展开
parameters:
  training.lr.values: [0.001, 0.0005, 0.0001]
  model.dropout.values: [0.1, 0.3, 0.5]

# 2. 显式候选列表（参数成组变化时使用）
candidates:
  - training.lr: 0.001
    model.dropout: 0.1
  - training.lr: 0.0005
    model.dropout: 0.3
```

inner checkpoint 不会被直接复用到 outer-test final evaluation，outer final 会重新训练。

### 结果解读

```
更像"带选择过程和 final retrain 的无偏验证"
重点看 outer-test 结果、选中参数、final retrain 结果和参数选择稳定性
```

---

## 推荐使用顺序

```
P2 或小规模 P1 smoke
    ↓  排除明显不稳、crop 敏感、holdout 崩溃的路线
P1 主比较
    ↓  看平均表现、fold_std、subject_std、all-correct rate
P3 最终验证
    ↓  最贵，不是默认入口
```

---

## 输出物要求

提交真实 candidate 结论时必须具备：

- 正式 dataset manifest
- 正式 split manifest
- held-out test prediction / score matrix
- run manifest
- candidate audit report
- 绑定 audit/manifest 的 route summary
