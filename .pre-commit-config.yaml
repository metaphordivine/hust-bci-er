repos:
  - repo: local
    hooks:
      - id: repo-doctor-fast
        name: Run fast repository doctor
        entry: python scripts/repo_doctor.py fast
        language: system
        pass_filenames: false
